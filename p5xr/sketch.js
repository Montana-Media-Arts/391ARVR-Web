function preload() {
    createVRCanvas();
}

function setup()
{
  setVRBackgroundColor(0, 200, 0);
}

function calculate() {
    // code that you want to run once each frame.
  
 
}

function draw()
{
      // fill(0,0,255);
    //circle(0,0,2);
}
