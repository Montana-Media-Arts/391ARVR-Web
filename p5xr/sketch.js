function preload() {
    createVRCanvas();
}

function setup()
{
  
}

function calculate() {
    // code that you want to run once each frame.
  setVRBackgroundColor(0, 255, 0);
 
}

function draw()
{
      // fill(0,0,255);
    //circle(0,0,2);
}
