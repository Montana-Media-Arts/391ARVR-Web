function preload() {
    createVRCanvas();
}

function setup()
{
  setVRBackgroundColor(100, 200, 150);
}

function calculate() {
    // code that you want to run once each frame.
  
 
}

function draw()
{
       fill(0,0,255);
    circle(0,0,2);
}
