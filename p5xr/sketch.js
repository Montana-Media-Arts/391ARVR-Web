function preload() {
    createVRCanvas();
}

function setup()
{

}

function calculate() {
    // code that you want to run once each frame.
    setVRBackgroundColor(200, 0, 150);
    circle(100,100,25);
}